[user]
    name = n6o
    email = 16708063+n6o@users.noreply.github.com

[include]
    path = config.local

[includeIf "gitdir:~/src/github.com/n6o"]
  path = config.n6o

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[ghq]
    vcs = git
    root = ~/src

[delta]
    line-numbers = true
    side-by-side = true